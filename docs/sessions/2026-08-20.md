# 2026-08-20

## Goal

对 Rasen change `stabilize-capture-location-and-workday` 做一次冷重建核验，保留当前工作树改动，确认拍照、定位、工作日规则和测试配置的编译/验证边界。

## Change Summary

- 当前工作树已经包含 CaptureContext、相机 single-flight、bounded fresh-location、定位 generation/lifecycle、Photos 元数据兜底、2026 年官方节假日/调休和三态上下班状态实现。
- 当前工作树已经把测试宿主切到 `DayMark.app`/`DayMark`，并添加共享 `WorkStamp` scheme，包含 `WorkStampTests` 与 `WorkStampUITests`。
- 本次 successor 核验未修改上述源码、工程配置或测试；仅补建本会话日志。未提交、未推送、未执行 `git add -A`。

## Verification

- `rasen status --change "stabilize-capture-location-and-workday" --json`：`spec-driven`，16 项中 15 项已勾选，唯一未完成项为 2.4。
- `rasen instructions apply --change "stabilize-capture-location-and-workday" --json`：状态 `ready`，上下文文件和任务路径均可读取。
- `/Applications/Xcode-beta.app/Contents/Developer/usr/bin/xcodebuild -project WorkStamp.xcodeproj -list`：exit 0；发现 `WorkStamp` scheme 以及 `WorkStamp`、`WorkStampTests`、`WorkStampUITests` 三个 target。
- `/Applications/Xcode-beta.app/Contents/Developer/usr/bin/xcodebuild -project WorkStamp.xcodeproj -scheme WorkStamp -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/WorkStampReviewDerivedData build`：exit 0；仅观察到已记录的 iOS 26 `CLGeocoder` 弃用警告。
- `/Applications/Xcode-beta.app/Contents/Developer/usr/bin/xcodebuild -project WorkStamp.xcodeproj -scheme WorkStamp -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/WorkStampReviewTestDerivedData build-for-testing`：exit 0；DayMark.app、WorkStampTests 和 WorkStampUITests runner 均完成构建。
- `env DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer /usr/bin/xcrun simctl list devices available`：exit 0；列出 iOS 27 iPhone 17，但状态为 Shutdown。
- `SWIFT_STRICT_CONCURRENCY=complete /Applications/Xcode-beta.app/Contents/Developer/usr/bin/xcodebuild -project WorkStamp.xcodeproj -scheme WorkStamp -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/WorkStampStrictReviewData build`：exit 65，约 11 秒结束且未启动模拟器。编译到 `LocationService.swift`、`PhotoLibrarySaver.swift` 时只观察到已有的 iOS 26 `CLGeocoder`/`cancelGeocode()` 弃用警告，未观察到 `CameraService`、`LocationService` 或 `PhotoLibrarySaver` 的 service-level strict-concurrency warning。
- `env DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer /usr/bin/xcrun devicectl list devices`（提升权限）：发现已配对真机“哥谭之王”，型号 iPhone Air，设备 ID `A67DE9B5-C985-51C3-83BE-FBC006C114A4`。
- 真机 Debug 构建：`xcodebuild -project WorkStamp.xcodeproj -scheme WorkStamp -configuration Debug -destination id=A67DE9B5-C985-51C3-83BE-FBC006C114A4 -derivedDataPath /tmp/WorkStampDeviceDerivedData build`：exit 0，签名与安装包校验通过。
- `devicectl device install app`：`com.godmiracle.WorkStamp` 安装成功；随后 `devicectl device process launch --terminate-existing` 启动成功。
- 真机测试：`xcodebuild -project WorkStamp.xcodeproj -scheme WorkStamp -destination id=A67DE9B5-C985-51C3-83BE-FBC006C114A4 -derivedDataPath /tmp/WorkStampDeviceTestDerivedData -resultBundlePath /tmp/WorkStampDeviceTests.xcresult -only-testing:WorkStampTests -only-testing:WorkStampUITests test`：exit 0；`WorkStampTests` 16/16 通过，`WorkStampUITests` 1/1 通过。

## Blocked Tests

按用户要求，在观察到 CoreSimulator 环境阻塞后没有继续启动新的模拟器命令。执行过的具体命令为：

```sh
env DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer /Applications/Xcode-beta.app/Contents/Developer/usr/bin/xcodebuild -project WorkStamp.xcodeproj -scheme WorkStamp -destination 'platform=iOS Simulator,id=7E00CE17-A0E0-45F1-BDED-BE0DF82EC640' -derivedDataPath /tmp/WorkStampReviewTestRunData -only-testing:WorkStampTests test
```

该命令先报告：`CoreSimulatorService connection became invalid`、`Error Domain=NSPOSIXErrorDomain Code=61 "Connection refused"`、`Unable to discover any Simulator runtimes`；最终 exit 70，确切错误为：

```text
xcodebuild: error: Unable to find a device matching the provided destination specifier:
        { platform:iOS Simulator, id:7E00CE17-A0E0-45F1-BDED-BE0DF82EC640 }
The requested device could not be found because no available devices matched the request.
```

没有 XCTest worker 或 unit/UI runtime 通过证据，因此不宣称测试通过。本次严格 generic build 已实际运行并在宏插件错误处终止；任务 2.4 继续保持未勾选。planning-context 中记载的宏沙箱问题也由本次相同类型的错误再次复现。

## Remaining Risks

- 已获得真机安装、启动、unit test 和 UI tap-through 证据；现有 UI 测试只覆盖拍照页控件/设置入口，不等价于真实相机取景、拍照、定位刷新和 Photos 写入全链路验收。
- 本次 strict generic build 未得到完整 source-level 结果：`SettingsView.swift` 编译时出现 `sandbox-exec: sandbox_apply: Operation not permitted`，随后 `SwiftUIMacros.StateMacro` 和 `PreviewsMacros.SwiftUIView` 报 malformed plugin response；需在工具链环境恢复后重跑，并继续禁止 `@preconcurrency`/诊断抑制作为替代。
- 由于严格构建未完成 source-level 结果且没有出现可安全修复的服务级并发警告，本次未修改源码；后续 review 应继续重点检查 LocationService 的 callback/continuation 终态、相机晚到回调和 Photos 资源写入的真机行为。
