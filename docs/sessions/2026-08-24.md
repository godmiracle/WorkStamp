# 2026-08-24

## Session Goal

在设置页加入版本信息，便于通过手机上的安装包核对当前代码是否为最新版本。

## What Changed

- 新增“关于版本”卡片，显示应用版本、构建号和 Debug / Release 构建渠道。
- 从 Bundle 读取 `CFBundleShortVersionString` 与 `CFBundleVersion`，避免在 UI 中重复维护版本文本。
- 将工程构建号从 `2` 提升为 `3`。
- 同步更新版本记录、发布清单、待办、决策和变更日志。

## Verification

- Debug 模拟器 `build-for-testing`：通过；生成 `DayMark.app`、`WorkStampTests` 和 `WorkStampUITests`。
- 生成产物核对：`CFBundleShortVersionString=1.0`、`CFBundleVersion=3`。
- iPhone Air 真机 Debug 构建、安装和启动：通过；安装包版本为 `1.0 (3)`。
- 用户真机确认设置页版本号显示正常；真机单元测试本轮因设备锁定未执行，模拟器测试因 CoreSimulatorService 连接拒绝未执行。
