# 2026-08-25

## 原始定位首条回调与系统地图链路诊断

### 用户目标

确认上海北大科技园现场看到逸景佳苑时，是否经纬度从第一条 Core Location 回调开始就已经偏移，并区分 Core Location 融合落点与地址/POI 解析结果。

### 完成内容

- 读取 `LocationService` 的定位管理器、候选晋级、`CLGeocoder`、MapKit 反查和附近 POI 链路。
- 为 `LocationDiagnostics` 增加首条/最新原始样本、样本数、首末位移，以及系统地址和 MapKit 候选诊断。
- 增加对应模型回归测试，并完成 iPhoneOS arm64 `build-for-testing`。
- 通过命令行签名、安装和启动诊断包；未打开 Xcode 图形界面。

### 真机证据

- 设备：已解锁 iPhone Air（“哥谭之王”）。
- 首条原始样本：`31.323805, 121.481397`，`2026-08-25 11:01:54`。
- 最新原始样本：`31.323805, 121.481397`。
- 样本数：`2`；首末位移：约 `0m`。
- 精度：水平约 `±7m`，垂直约 `±30m`。
- 源标记：系统定位；软件模拟、外接设备均为“否”。
- 页面坐标与原始坐标一致，地址仍为逸景佳苑。
- 地址链路摘要显示系统地址已返回逸景佳苑，MapKit 候选等待约 35 秒仍未返回；本轮没有证据表明 MapKit 候选把科技园改成逸景佳苑。

### 未完成 / 下一步

- 本轮已通过顶部摘要确认 `CLGeocoder` 系统地址先返回逸景佳苑；MapKit 候选在观察窗口内未返回，因此仍不能评价其是否包含上海北大科技园候选。
- `CLLocationSourceInformation` 不公开 GNSS/Wi-Fi/蜂窝/室内具体融合来源；需用系统照片详情页地图落点或独立地图坐标建立参考点，再做同点多次采样。
- 当前证据已经足以停止地址解析层坐标修正；下一步优先查设备定位环境和系统落点对照。

### 验证

- `xcodebuild build`：通过。
- `xcodebuild build-for-testing`：通过。
- 真机安装/冷启动/面板读取：通过。
- 最终命令行真机测试：`31/31` 通过，0 失败、0 跳过；结果包为 `/tmp/WorkStampRawTraceDeviceTests2/Logs/Test/Test-WorkStamp-2026.08.25_11-08-54-+0800.xcresult`。第二轮测试期间设备曾因锁屏暂停，解锁后继续完成。

## 三类公开解析原始返回记录

### 实现

- 新增 `LocationResolverStatus`，区分尚未发起、等待返回、已返回、无结果和失败。
- `LocationDiagnostics` 现在保留 `CLGeocoder` 的全部 `CLPlacemark` 字段、MapKit 反查的全部 `MKMapItem` 字段，以及附近 POI 每次搜索半径下的全部候选和距离。
- 地点面板新增“解析原始返回”区域，分别展示三条链路的输入坐标、结果数量、原始地址字段、候选坐标、距离、搜索半径和错误信息。
- 附近 POI 扩大搜索半径时保留每次尝试，不再只保留最后一个候选。
- 原始诊断仅用于观测，不会修改 `LocationValue`、当前快照坐标或照片元数据。

### 测试

- 先新增 `locationDiagnosticsRetainsRawResolverResultsAndNearbyDistances` 回归测试，确认三类结果可同时保存。
- 真机命令行测试：`32/32` 通过，0 失败、0 跳过。
- 结果包：`/tmp/WorkStampRawResolverDeviceTests/Logs/Test/Test-WorkStamp-2026.08.25_13-44-27-+0800.xcresult`。
- 模拟器构建仍受 CoreSimulatorService / SwiftUI 宏插件环境故障影响；generic iPhoneOS 真机目标已完成源码编译并进入真机测试。

### 下一步真机采样

- 打开地点面板，展开“原始定位诊断”，在上海北大科技园连续刷新。
- 记录三类结果是否分别出现“逸景佳苑”“上海北大科技园”或无结果，并同时记录原始坐标与候选距离。
